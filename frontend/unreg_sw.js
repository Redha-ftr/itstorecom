navigator.serviceWorker.getRegistrations().then(function (registrations) { if (!registrations.length) {	console.log('No serviceWorker registrations found.');	return; } for(let registration of registrations) {	registration.unregister().then(function (boolean) { console.log(	(boolean ? 'Successfully unregistered' : 'Failed to unregister'), 'ServiceWorkerRegistration' +	(registration.installing ? ' .installing.scriptURL = ' + registration.installing.scriptURL + '' : '') +	(registration.waiting ? ' .waiting.scriptURL = ' + registration.waiting.scriptURL + '' : '') +	(registration.active ? ' .active.scriptURL = ' + registration.active.scriptURL + '' : '') +	' .scope: ' + registration.scope + '' );	}); }});